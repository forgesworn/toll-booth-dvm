/** NIP-90 kind for generic paid HTTP API proxy requests. */
export const JOB_KIND = 5800

/** NIP-90 result kind (JOB_KIND + 1000). */
export const RESULT_KIND = 6800

/** NIP-90 feedback kind (status updates, payment requests). */
export const FEEDBACK_KIND = 7000

/** NIP-89 application handler kind (DVM discovery). */
export const HANDLER_KIND = 31990

/** Kind 31402 paid API service announcement (NIP-PAID-SERVICES). */
export const PAID_SERVICES_KIND = 31402
